<Window x:Class="AutVecu.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:AutVecu.Desktop.Views.Controls"
        mc:Ignorable="d"
        Title="AUT vECU Test Automation"
        Height="760"
        Width="1160"
        MinHeight="680"
        MinWidth="1040"
        Background="#F8FAFC">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1.25*" />
            <RowDefinition Height="1.45*" />
        </Grid.RowDefinitions>

        <controls:DashboardHeader Grid.Row="0" Margin="0,0,0,16" />

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.9*" />
                <ColumnDefinition Width="1.35*" />
            </Grid.ColumnDefinitions>

            <controls:VecuServerPanel Grid.Column="0" Margin="0,0,14,0" />
            <controls:TestInputPanel Grid.Column="1" />
        </Grid>

        <controls:LiveTrafficPanel Grid.Row="2" Margin="0,0,0,14" />
        <controls:ResultsAndLogsPanel Grid.Row="3" />
    </Grid>
</Window>
