<UserControl x:Class="AutVecu.Desktop.Views.Controls.TestInputPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource PanelBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="78" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.ColumnSpan="3"
                       Style="{StaticResource SectionTitle}"
                       Foreground="#0B66C3"
                       Text="Test Input" />

            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Text="Mode:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,14" />
            <ComboBox Grid.Row="1"
                      Grid.Column="1"
                      Height="38"
                      Margin="0,0,12,14"
                      ItemsSource="{Binding TestModes}"
                      SelectedItem="{Binding SelectedTestMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="2"
                       Grid.Column="0"
                       Text="Input:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,14" />
            <TextBox Grid.Row="2"
                     Grid.Column="1"
                     Height="38"
                     Margin="0,0,12,14"
                     Text="{Binding ScriptFolderPath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Row="2"
                    Grid.Column="2"
                    Width="104"
                    Height="38"
                    Margin="0,0,0,14"
                    Content="Browse"
                    Command="{Binding BrowseScriptFolderCommand}" />

            <StackPanel Grid.Row="3"
                        Grid.ColumnSpan="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center">
                <Button Content="Generate TCL"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding GenerateTclScriptsCommand}" />
                <Button Content="Run Tests"
                        Style="{StaticResource PrimaryButton}"
                        Margin="20,0,0,0"
                        Command="{Binding RunTestsCommand}" />
                <Button Content="History"
                        Width="96"
                        Height="38"
                        Margin="20,0,0,0"
                        Command="{Binding LoadHistoryCommand}" />
                <Button Content="Clear"
                        Width="86"
                        Height="38"
                        Margin="10,0,0,0"
                        Command="{Binding ClearLogsCommand}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

